#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <malloc.h>

#define N 1000
void Split(char* string, char* delimiter, char** tokens, int* tokensCount);

int main() {

}

void Split(char* string, char* delimiter, char** tokens, int* tokensCount) {
	int n = 0;
	int j = 0;
	for (int i = 0; i < strlen(string); i++)
	{
		while (string[i] != delimiter[j])
		{
			
		}
	}
}